# Supported image types
image_extensions = [".jpg", ".jpeg", ".png", ".gif", ".bmp", ".ico", ".svg"]

# Supported video types
video_extensions = [".mp4", ".avi", ".mov", ".mkv", ".flv"]

# Supported audio types
audio_extensions = [".mp3", ".wav", ".aac", ".flac"]

# Supported document types
document_extensions_word = [".doc", ".docx", ".odt"]
document_extensions_pdf = [".pdf"]
document_extensions_excel = [".xls", ".xlsx", ".csv"]
document_extensions_ppt = [".ppt", ".pptx"]

# Archive files
zip_File = [".zip", ".rar", ".tar.gz"]

# Applications
appications = [".exe", ".msi"]

# Define file types for C files
c_file = ['.c', '.h']  # .c source files and .h header files

# Define file types for Python files
python_file = ['.py']  # .py Python source files